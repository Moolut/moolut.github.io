---
import TVLayout from '../layouts/TVLayout.astro'
---

<TVLayout>
  <article class="welcome">
    <div class="hero-section">
      <h1 class="big-text glitchy-text hero-title">
        <span class="name-hover" tabindex="0">Mevlüt Yıldırım</span>
      </h1>
      <p class="hero-tagline">
        <span class="role-text">Cybersecurity Engineer </span>
        <span class="static-text">based in Munich</span>
      </p>
    </div>

    <div class="intro">
      <p class="intro-text">
        I am a
        <strong class="highlight">Cybersecurity Professional</strong> and
        <strong class="highlight">IoT Engineer</strong> specializing in
        <strong class="highlight">penetration testing</strong>,
        <strong class="highlight">embedded systems security</strong>, and building
        <strong class="highlight">secure IoT solutions</strong>. Currently
        completing my <strong class="highlight">M.Sc. in Communications and
        Electronics Engineering at TUM</strong> with a focus on
        <strong class="highlight-link" data-info="Advanced security protocols for connected devices">IoT Security</strong> and
        <strong class="highlight-link" data-info="Firewalls, IDS/IPS, VPN, and network hardening">Network Security</strong>.
      </p>
    </div>

    <section class="what-drives-me">
      <h2 class="section-title">What Drives Me</h2>
      <div class="drives-grid">
        <div class="drive-card">
          <h3 class="drive-title">Security-First Mindset</h3>
          <p class="drive-description">
            I thrive on uncovering hidden vulnerabilities and strengthening
            systems through rigorous <strong class="highlight-link" data-info="OWASP Top 10, API testing, exploit development">penetration testing</strong> and
            <strong class="highlight">threat analysis</strong>, ensuring robust protection against
            emerging cyber threats.
          </p>
        </div>
        <div class="drive-card">
          <h3 class="drive-title">Innovation in IoT Security</h3>
          <p class="drive-description">
            Passionate about developing cutting-edge security solutions for
            <strong class="highlight-link" data-info="Firmware analysis, hardware hacking, ARM debugging">embedded systems</strong> and <strong class="highlight">IoT devices</strong>,
            bridging the gap between hardware and software security.
          </p>
        </div>
        <div class="drive-card">
          <h3 class="drive-title">Continuous Learning</h3>
          <p class="drive-description">
            Actively expanding expertise through hands-on labs, <strong class="highlight-link" data-info="IOTSEC CTF, Kaspersky CTF, Hack The Box">CTF
            competitions</strong>, and research, staying ahead of
            <strong class="highlight">AI-driven threats</strong> and advanced attack vectors in
            cybersecurity.
          </p>
        </div>
        <div class="drive-card">
          <h3 class="drive-title">Real-World Impact</h3>
          <p class="drive-description">
            Dedicated to applying offensive security techniques and
            <strong class="highlight-link" data-info="CVE research, security audits, risk assessment">vulnerability assessment</strong> to protect critical
            systems, from <strong class="highlight">aerospace networks</strong> to <strong class="highlight">smart infrastructure</strong>.
          </p>
        </div>
      </div>
    </section>

    <div class="call-to-action-section">
      <p class="call-to-action">
        <a href="/about/">Learn more about me</a> or check out my
        <a href="/projects/">projects</a> and
        <a href="/certifications/">achievements</a>.
      </p>
    </div>
  </article>
</TVLayout>

<style>
  .welcome {
    max-width: 100%;
  }

  /* Hero Section */
  .hero-section {
    margin-bottom: 3rem;
  }

  .hero-title {
    margin: 0 0 1rem 0;
    text-transform: uppercase;
  }

  .hero-tagline {
    font-size: clamp(16px, 2vw, 22px);
    line-height: 1.5;
    margin: 0 0 2rem 0;
    min-height: 2em;
  }

  /* Role text with rainbow-static effect */
  .role-text {
    display: inline-block;
    position: relative;
    color: var(--text-secondary);
    font-weight: 600;
    background-position: center center;
    background-size: 90% 0;
    background-repeat: no-repeat;
    transition: background-size 0.4s cubic-bezier(0.29, 0.8, 0.21, 0.63);
  }

  html:not(.webp) .role-text {
    background-image: url(/assets/images/rainbow-static.gif);
  }

  html.webp .role-text {
    background-image: url(/assets/images/rainbow-static.webp);
  }

  .role-text.changing {
    background-size: 100% 100%;
  }

  .static-text {
    margin-left: 0.5ch;
  }

  /* Name hover effect to show profile photo */
  .name-hover {
    position: relative;
    color: var(--text-secondary);
  }

  .name-hover::after {
    content: '';
    display: block;
    position: absolute;
    z-index: 100;
    top: calc(100% + 1em);
    left: 1rem;
    width: 14rem;
    padding-bottom: 14rem;
    visibility: hidden;
    background-size: cover;
    background-position: center;
    border: 2px solid var(--text-secondary);
  }

  .name-hover:hover::after,
  .name-hover:focus::after {
    background-image: url(/assets/images/profile.png);
    transition: visibility 0s 0.2s;
    visibility: visible;
  }

  .intro {
    margin-top: 2rem;
    margin-bottom: 3rem;
  }

  .intro-text {
    font-size: clamp(14px, 1.6vw, 18px);
    line-height: 1.7;
    margin-bottom: 0;
  }

  /* Basic keyword highlighting */
  .highlight {
    color: var(--text-secondary);
    font-weight: 600;
  }

  /* Interactive highlight with hover effect */
  .highlight-link {
    color: var(--text-secondary);
    font-weight: 600;
    position: relative;
    cursor: help;
    border-bottom: 1px dotted var(--text-secondary);
    transition: all 0.3s ease;
  }

  .highlight-link:hover {
    color: #fff;
    text-shadow: 0 0 10px var(--text-secondary);
  }

  .highlight-link::before {
    content: attr(data-info);
    position: absolute;
    bottom: calc(100% + 0.5rem);
    left: 50%;
    transform: translateX(-50%);
    background: rgba(0, 0, 0, 0.95);
    color: var(--text-secondary);
    padding: 0.5rem 1rem;
    border-radius: 4px;
    border: 1px solid var(--text-secondary);
    font-size: 12px;
    white-space: nowrap;
    visibility: hidden;
    opacity: 0;
    transition: all 0.3s ease;
    pointer-events: none;
    z-index: 1000;
  }

  .highlight-link:hover::before {
    visibility: visible;
    opacity: 1;
  }

  /* Arrow for tooltip */
  .highlight-link::after {
    content: '';
    position: absolute;
    bottom: calc(100% + 0.5rem - 6px);
    left: 50%;
    transform: translateX(-50%) rotate(45deg);
    width: 8px;
    height: 8px;
    background: rgba(0, 0, 0, 0.95);
    border-right: 1px solid var(--text-secondary);
    border-bottom: 1px solid var(--text-secondary);
    visibility: hidden;
    opacity: 0;
    transition: all 0.3s ease;
    pointer-events: none;
    z-index: 999;
  }

  .highlight-link:hover::after {
    visibility: visible;
    opacity: 1;
  }

  /* What Drives Me Section */
  .what-drives-me {
    margin-bottom: 3rem;
  }

  .section-title {
    font-size: clamp(20px, 3vw, 32px);
    text-transform: uppercase;
    margin-bottom: 2rem;
    color: var(--text-secondary);
  }

  .drives-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  @media (min-width: 768px) {
    .drives-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .drive-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 1.5rem;
    border-radius: 4px;
    transition: all 0.3s ease;
  }

  .drive-card:hover {
    border-color: var(--text-secondary);
    background: rgba(255, 255, 255, 0.05);
    transform: translateY(-2px);
  }

  .drive-title {
    font-size: clamp(16px, 1.8vw, 20px);
    text-transform: uppercase;
    margin-bottom: 0.75rem;
    color: var(--text-secondary);
  }

  .drive-description {
    font-size: clamp(13px, 1.4vw, 16px);
    line-height: 1.6;
    margin: 0;
  }

  .drive-description .highlight,
  .drive-description .highlight-link {
    font-weight: 600;
  }

  /* Call to action */
  .call-to-action-section {
    margin-top: 2rem;
  }

  .call-to-action {
    font-size: clamp(14px, 1.6vw, 18px);
    opacity: 0.9;
    margin: 0;
  }

  @media (max-width: 580px) {
    .name-hover::after {
      width: 12rem;
      padding-bottom: 12rem;
      left: 50%;
      transform: translateX(-50%);
    }

    .highlight-link::before {
      white-space: normal;
      max-width: 200px;
      left: 0;
      transform: none;
    }

    .highlight-link::after {
      left: 2rem;
      transform: rotate(45deg);
    }
  }
</style>

<script>
  // Role cycling with rainbow-static effect
  const roles = [
    'Cybersecurity Engineer',
    'Penetration Tester',
    'Ethical Hacker',
    'Security Researcher'
  ];

  let roleIndex = 0;
  const roleElement = document.querySelector('.role-text');

  function cycleRole() {
    // Add changing class to trigger rainbow-static
    roleElement.classList.add('changing');

    setTimeout(() => {
      // Change role text
      roleIndex = (roleIndex + 1) % roles.length;
      roleElement.textContent = roles[roleIndex] + ' ';

      // Remove changing class after transition
      setTimeout(() => {
        roleElement.classList.remove('changing');
      }, 400);
    }, 200);
  }

  // Cycle every 3 seconds
  setInterval(cycleRole, 3000);
</script>
